%======£°£°£°£°IMPORTANT ◊¢“‚ºÏ≤È±ª ‘±‡∫≈”Îsession∫≈£°£°£°£°=====
subID = 3; % Ã∆…‹ª™
sessID = 1;
runID = 1;

%% run1: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run2: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+1;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run3: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+2;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run4: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+3;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run5: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+4;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end
%% run6: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+5;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run7: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+6;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run8: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+7;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run9: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+8;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run10: MRI train 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+9;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItrain(subID,sessID,currentRunID);
else
    disp('check again')
end
