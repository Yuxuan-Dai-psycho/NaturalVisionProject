%%======！！！！IMPORTANT 注意检查被试编号与session号！！！！=====
subID = 4; % shenhuadong 沈华东32
sessID = 1;
runID = 1;

%% run1: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run2: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+1;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run3: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+2;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run4: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+3;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run5: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+4;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end
%% run6: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+5;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run7: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+6;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run8: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+7;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run9: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+8;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end

%% run10: MRI test 
clc;sca;
cd('D:\Exp\BrainImageNet');
currentRunID = runID+9;
sprintf('sub ID %d, sess ID %d, run ID %d, please check!', subID, sessID, currentRunID)
check = input('y/n:', 's');
if check == 'y'
    binMRItest(subID,sessID,currentRunID);
else
    disp('check again')
end
